{% extends 'base.html.twig' %}
{% block body %}
    <h1>Results</h1>
    <p>
        <a href="{{ path('lab2_setup') }}">Setup</a> |
        <a href="{{ path('lab2_request') }}">Dashboard</a> |
        <a href="{{ path('lab2_reset') }}">Reset</a>
    </p>

    <h2>Tally</h2>
    <pre>{{ dump(tally) }}</pre>

    <h2>Used IDs (published list)</h2>
    <pre>{{ dump(usedIds) }}</pre>

    <h2>Log</h2>
    <pre>{{ log|join("\n") }}</pre>
{% endblock %}

1. Писати додатковий лог у ConsumeProviderEventCommandHandler - не всі запити до нього доходять, не контролює інформацію про відповідь
2. Перенести логування у ConsumeProviderEventStaticController та ConsumeProviderEventDynamicController
\PayHub\Integration\Contract\ProviderEvent\ProviderEventMapperInterface::normalizeEventPayload - деякі нормалайзеру відразу містять і валідацію
